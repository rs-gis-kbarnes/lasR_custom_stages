% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stages.R
\name{tree_hull3d}
\alias{tree_hull3d}
\title{3D concave hull per tree ID}
\usage{
tree_hull3d(
  attribute = "tree_id",
  max_edge = 0,
  radius = 0,
  wire_filter = "",
  ofile = tempgpkg()
)
}
\arguments{
\item{attribute}{character. Name of the point attribute holding the integer tree segmentation
ID (see \code{add_extrabytes()} + \code{transform_with()} to project a \code{region_growing()} raster onto
points). Must be an attribute of type \code{int} or \code{double}; other types raise an error.}

\item{max_edge}{numeric. Maximum triangle edge length threshold used to trim the Delaunay
triangulation before extracting the contour, exactly as in \code{triangulate()}. \code{0} (default)
keeps every triangle, i.e. produces the convex hull of each tree's points.}

\item{radius}{numeric. If greater than \code{0}, restrict output to only the tree IDs that have at
least one point matching \code{wire_filter} within this radius (map units). \code{0} (default) disables
this restriction and processes every tree ID present in the point cloud.}

\item{wire_filter}{character. A point filter string (same DSL as the \code{filter} argument
elsewhere in lasR, e.g. \code{keep_class(14)}) used to flag "wire" points when \code{radius > 0}.
Ignored if \code{radius == 0}.}

\item{ofile}{character. Full outputs are always stored on disk. If \code{ofile = ""}
then the stage will not store the result on disk and will return nothing. It will however
hold partial output results temporarily in memory. This is useful for stage that are only
intermediate stage.}
}
\value{
This stage produces a vector. The path provided to `ofile` is expected to be `.gpkg` or
any other format supported by GDAL. Vector stages may produce geometries with Z coordinates.
Thus, it is discouraged to store them in formats with no 3D support, such as shapefiles.
}
\description{
Groups points by a tree segmentation ID stored in a point attribute (e.g. produced by
\code{region_growing()} and projected onto points with \code{transform_with()}), triangulates each
group independently with a Delaunay triangulation, and extracts its contour as in \code{hulls()}.
Because each tree is triangulated separately, the output is a closed, three-dimensional
polygon per tree (Z is taken from the triangulated vertex heights) rather than a single
global 2D contour. Setting \code{max_edge} produces a concave hull instead of a full convex hull,
exactly as in \code{triangulate()}/\code{hulls()}. Optionally, a \code{radius} and \code{wire_filter} restrict
processing to only the tree IDs that have at least one matching "wire" point within \code{radius}
map units, skipping every other tree entirely.
}
\examples{
g <- system.file("extdata", "MixedConifer.las", package = "lasR")
chm  <- rasterize(1, "max", filter = keep_first())
lmx  <- local_maximum_raster(chm, 5)
tree <- region_growing(chm, lmx, max_cr = 10)
eb   <- add_extrabytes("int", "tree_id", "Tree segmentation ID")
attr <- transform_with(tree, operator = "=", store_in_attribute = "tree_id", bilinear = FALSE)
hull3d <- tree_hull3d(attribute = "tree_id", max_edge = 3)
pipeline <- chm + lmx + tree + eb + attr + hull3d
ans <- exec(pipeline, on = g)

}
\seealso{
\link{hulls}
\link{triangulate}
\link{region_growing}
\link{transform_with}
}
